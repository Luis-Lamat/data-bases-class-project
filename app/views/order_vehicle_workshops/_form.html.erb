<%= form_for(@order_vehicle_workshop) do |f| %>
  <% if @order_vehicle_workshop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_vehicle_workshop.errors.count, "error") %> prohibited this order_vehicle_workshop from being saved:</h2>

      <ul>
      <% @order_vehicle_workshop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vehicle_id %><br>
    <%= f.collection_select :vehicle_id, Vehicle.all, :id, :vehicle_info %>
  </div>
  <div class="field">
    <%= f.label :workshop_id %><br>
    <%= f.collection_select :workshop_id, Workshop.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.collection_select :order_id, Order.all, :id, :order_info %>
  </div>

  <div class="actions">
    <%= f.submit class: "button medium success" %>
  </div>
<% end %>
