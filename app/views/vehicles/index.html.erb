<h1>Listing vehicles</h1>

<table>
  <thead>
    <tr>
      <th>Series num</th>
      <th>Model</th>
      <th>Brand</th>
      <th>Color</th>
      <th>Placas</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vehicles.each do |vehicle| %>
      <tr>
        <td><%= vehicle.series_num %></td>
        <td><%= vehicle.model %></td>
        <td><%= vehicle.brand %></td>
        <td><%= vehicle.color %></td>
        <td><%= vehicle.placas %></td>
        <td><%= link_to 'Show', vehicle %></td>
        <td><%= link_to 'Edit', edit_vehicle_path(vehicle) %></td>
        <td><%= link_to 'Destroy', vehicle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Vehicle', new_vehicle_path, class: "button medium success" %>
